# ml_project_unipi
ML project repository for the unipi Machine Learning 25/26 project type B

Create a vev
python3 -m venv .venv

source .venv/bin/activate

# install requirements
pip3 install -r requirements.txt 